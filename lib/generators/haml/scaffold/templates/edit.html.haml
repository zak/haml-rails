#<%= singular_table_name %>
  %h1 Editing <%= singular_table_name %>

  = render 'form'

  = link_to t('link.show'), @<%= singular_table_name %>
  \|
  = link_to t('link.back'), <%= index_helper %>_path
